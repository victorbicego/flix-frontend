import { ConvertDurationPipe } from './convert-duration.pipe';

describe('ConvertDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
