import { ConvertDescriptionPipe } from './convert-description.pipe';

describe('ConvertDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
